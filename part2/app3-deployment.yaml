apiVersion: v1
kind: ConfigMap
metadata:
  name: app3-config
data:
  index.html: |
    <!DOCTYPE html>
    <html>
    <head>
        <title>App 3</title>
    </head>
    <body>
        <h1>Bienvenue sur app3.com !</h1>
        <p>Ceci est l'application numero 3.</p>
    </body>
    </html>
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: app3-deployment
spec:
  replicas: 1 # Nombre de pods (instances) à lancer
  selector:
    matchLabels:
      app: app3 # Doit correspondre au 'selector' de ton app3-service.yaml
  template:
    metadata:
      labels:
        app: app3 # Doit correspondre au 'selector' de ton app3-service.yaml
    spec:
      containers:
      - name: nginx-app3
        image: nginx:alpine # Image légère de Nginx
        ports:
        - containerPort: 80 # Le port sur lequel Nginx écoute dans le conteneur
        volumeMounts:
        - name: html-volume
          mountPath: /usr/share/nginx/html # Dossier où Nginx lit ses pages web
      volumes:
      - name: html-volume
        configMap:
          name: app3-config # On lie le ConfigMap créé au-dessus à ce volume
